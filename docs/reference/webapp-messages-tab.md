# Webapp: Messages tab

The workspace webapp shows workflow messages in the **Messages** tab.

## Message body formatting

Message bodies render as Markdown. This means common Markdown constructs display as their corresponding HTML elements (for example, headings, bold text, lists, and fenced code blocks).

## Notes

- Markdown rendering applies to the message body content.
